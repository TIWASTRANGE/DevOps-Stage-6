---
# Handlers for deploy role

- name: restart docker containers
  community.docker.docker_compose:
    project_src: "{{ app_dir }}"
    restarted: yes

- name: rebuild docker containers
  community.docker.docker_compose:
    project_src: "{{ app_dir }}"
    build: yes
    restarted: yes